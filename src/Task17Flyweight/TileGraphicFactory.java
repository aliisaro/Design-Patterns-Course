package Task17Flyweight;

// Create a TileGraphicFactory that will manage and return the graphical representation for each unique tile type.
// This factory should:
// Check if a graphic for a specific tile type already exists and, if so,
// return the existing graphic. If a graphic doesn’t exist for the tile type,
// create it, store it, and then return it. This factory will ensure only one instance of
// each tile graphic is created and shared across multiple tiles of the same type.

public class TileGraphicFactory {
}
